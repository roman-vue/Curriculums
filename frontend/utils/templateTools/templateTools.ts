import TempateToolsInterface from "@/utils/templateTools/templateTools.d";

const templateTools: TempateToolsInterface = {
  sections: ["cv sections", "elements"],
};

export const section = {
  cvSentions: "cv sections",
  elements: "elements",
};

export default templateTools;
